return {
	cmp = require("keys.cmp").setup,
	dap = require("keys.dap").setup,
	telescope = require("keys.telescope").setup,
	gitsigns = require("keys.gitsigns").setup,
	global = require("keys.global").setup,
}
